__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/

# training artefacts: policies are outputs, not source
runs/
*.zip
*.pt
*.pth

# TacView recordings
*.acmi

# The answer key.  A student repo is downstream of this one, so once this lands
# in history it cannot be taken back out.  Moves to a private submodule later.
solutions/

# Superseded experiments, kept on disk for reference only.  141 MB of old
# checkpoints does not belong in anyone's clone.
archived/
