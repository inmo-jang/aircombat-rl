"""Things you point at the environment.

`manual_operation` is the hand-flying tool the package README documents -- both
control layers, the instrument panel and TacView.  It ships with the gym because
flying the interface by hand is part of learning what the interface is.

Tools that *measure* the environment rather than drive it are outside the
package, in `tests/`.
"""
