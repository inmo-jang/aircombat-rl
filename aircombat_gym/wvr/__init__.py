"""Within visual range: the gun task.

`spaces` is the frozen obs/action spec, `engagement` is the weapon, and
`baselines` holds the scripted opponents written against it.  A `bvr` package
sits beside this one when missiles arrive -- same aircraft, same guidance,
different weapon, different observation and different bots.
"""
